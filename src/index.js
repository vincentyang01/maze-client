document.addEventListener("DOMContentLoaded", () => {

    //Runtime

    // const mazeFetchAdapter = new MazeFetchAdapter("http://localhost:3000/")

    //Fetch all users and log their names

    const action = users => users.forEach(console.log)

    // mazeFetchAdapter.get("maze", action)
})